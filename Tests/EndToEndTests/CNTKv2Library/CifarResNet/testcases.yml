dataDir: .

tags:
    - bvt-e (device == 'gpu') and ((flavor == 'release') if (os == 'windows') else (flavor == 'debug'))
    - nightly-e (device=='gpu') or (flavor== 'release')

testCases:
  Test run must be completed:
    patterns:
      - "CNTKv2Library-CifarResNet tests: Passed"

